为了适配仅支持节点输入的 GNN 模型，我们需要将《分子图信息汇总.docx》中提到的边特征通过“邻接特征池化”转换为等效的节点属性。

以下是重写后的汇总，重点详述了两个池化特征的执行方案。

------

## 一、 节点特征分布汇总（原子级）

基于提供的文档，现有的节点特征及其对应的研究文献分布如下：

| **节点特征**                   | **对应文献来源**                                             |
| ------------------------------ | ------------------------------------------------------------ |
| **原子符号/序数**（如C、O、N） | EquiScore, DENVIS, KPGT, GraphDTA, E(n) Equivariant GNN, Drug--Target Prediction, DTA-GTOmega, NEK7筛选, TIPE2筛选, COVIDVS |
| **几何空间坐标**（三维坐标）   | EquiScore, E(n) Equivariant GNN                              |
| **手性标签**                   | DENVIS                                                       |
| **形式电荷**                   | DENVIS                                                       |
| **自由基电子数量**             | DENVIS                                                       |
| **杂化状态**                   | DENVIS, DTA-GTOmega（药物部分）                              |
| **芳香性标记**                 | DENVIS, GraphDTA, NEK7筛选                                   |
| **环结构标记**                 | DENVIS, NEK7筛选                                             |
| **隐式价电子值**               | GraphDTA                                                     |

------

## 二、 边特征分布汇总（化学键级）

以下边特征将作为“池化转换”的数据源：

| **边特征**                               | **对应文献来源**                                             |
| ---------------------------------------- | ------------------------------------------------------------ |
| **化学键类型**（如单键、双键、共轭键等） | EquiScore, DENVIS, KPGT（原分子图边）, GraphDTA, E(n) Equivariant GNN, DTA-GTOmega（药物部分）, NEK7筛选, TIPE2筛选, COVIDVS |
| **键立体化学**（顺式、反式等）           | DENVIS                                                       |

------

## 三、 池化边特征（Pooled Edge Features）执行方案

通过对上述边特征进行局部聚合（Aggregation），我们为每个节点新增以下两个特征向量，实现“边信息节点化”。

### 1. 化学键类型计数向量（Bond Type Counting）

**执行逻辑：** 统计与当前原子节点 $v$ 直接相连的所有化学键类型数量。这种方法将原本属于“边”的类型属性（如单键、双键 ）转化为描述原子“化学环境”的统计指标。



- **向量构造**：构建一个 4 维向量 $[n_{single}, n_{double}, n_{triple}, n_{aromatic}]$。
- **计算方法**：
  - 遍历节点 $v$ 的邻接边。
  - 若边类型为单键（Single），$n_{single} + 1$。
  - 若边类型为双键（Double），$n_{double} + 1$。
  - 若边类型为叁键（Triple），$n_{triple} + 1$。
  - 若边类型为芳香键（Aromatic），$n_{aromatic} + 1$。
- **示例**：对于乙烯（$CH_2=CH_2$）中的碳原子，其连接了一个双键（C=C）和两个单键（C-H），则该池化向量为 $[2, 1, 0, 0]$。

### 2. 键立体化学状态映射（Stereochemical State Mapping）

**执行逻辑：** 将化学键的顺反（Cis/Trans ）等空间配置信息显式化为原子的位置状态。由于立体化学是成对定义的，我们将原子标记为该立体配置的端点。



- **向量构造**：构建一个 3 维布尔/One-hot 向量 $[is\_cis, is\_trans, is\_none]$。
- **计算方法**：
  - 检查与节点 $v$ 相连的所有化学键。
  - 若其中任意一根键被定义为 **顺式 (Cis/Z)**，则第一位设为 `1`。
  - 若其中任意一根键被定义为 **反式 (Trans/E)**，则第二位设为 `1`。
  - 若所有相连键均无立体化学定义，则第三位设为 `1`。
- **示例**：在顺-2-丁烯中，参与双键连接的两个碳原子，其立体化学池化向量均为 $[1, 0, 0]$。

------

## 四、 方案集成说明

将上述两个池化特征（共 7 维）直接拼接（Concatenate）在原有的节点特征向量（如原子序数、杂化状态等 ）之后。



$$X_{v, final} = [X_{atom\_base} \parallel V_{bond\_type} \parallel V_{stereo}]$$

通过这种方式，即使是不具备边信息处理能力的 GNN（如标准 GCN），也能通过节点特征感知到分子中的化学键饱和度及空间异构信息。

我可以为您提供基于 RDKit 将这些特征自动提取并转换为 PyTorch 张量的 Python 脚本，您需要吗？